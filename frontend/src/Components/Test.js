import { Menu, Dropdown, Row, Col } from 'antd';
import { DownOutlined } from '@ant-design/icons';
import React from 'react'

export const Test = () => {

     const menu = (
       
        <div>smvnsm</div>
     );

    return (
        <div>
           <h1>jhdjhdjdh</h1>
            {/* <Dropdown overlay={menu}> <a className="ant-dropdown-link" onClick={e => e.preventDefault()}>Hover me</a></Dropdown> */}
             
            
        </div>
    )
}
